name: workflow
on:
  push:
    branches: 
      - main
jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: print a welcome message
        run: echo "welcome to our repo"
      - name: checkout repo
        uses: actions/checkout@v6.1.0
      - name: list files
        run: ls -la
